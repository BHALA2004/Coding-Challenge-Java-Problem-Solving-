class Solution {
    public long[] findElements( long a[], long n)
    {
        // Your code goes here
        long[] s = new long[(int)n-2];
        Arrays.sort(a);
        for(int i=0;i<(n-2);i++){
            s[i]=(long)a[i];
        }
        return s;
    }
}
